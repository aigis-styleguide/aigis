machine:
  node:
    version: 4.2.2
dependencies:
  pre:
    - npm install -g bower
    - sudo apt-get install g++-4.8;
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 50
  override:
    - bower i
    - npm i
